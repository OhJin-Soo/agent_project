[project]
name = "agent-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "dotenv>=0.9.9",
    "ipython>=8.18.1",
    "langchain>=0.3.28",
    "langchain-anthropic>=0.3.22",
    "langgraph>=0.6.11",
]
